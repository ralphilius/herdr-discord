{
  "name": "herdr-discord",
  "version": "0.2.0",
  "private": true,
  "description": "Herdr plugin: Discord as a UI layer. Channels are workspaces, threads are agents.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/bot.js",
    "version": "node scripts/sync-version.js && git add herdr-plugin.toml"
  },
  "dependencies": {
    "discord.js": "^14.21.0"
  }
}
